#include <bits/stdc++.h>

using namespace std;
using lint = long long;

int main(void)
{
    int N;
    cin >> N;
    vector<int> color(N, 0);
    for(int i = 0; i < N; i++){
        int a, b, c;
        cin >> a >> b >> c;
        if(c % 2 == 0){
            
        }
    }
    return 0;
}